# TimeBar v2.3 ⏱️💰

> 把每一筆消費轉換成「退休時間成本」，讓你更清楚每個財務決策對未來的影響。

## ✨ 功能特色

### 核心計算
- **時間成本轉換**：將每筆花費/儲蓄轉換為「延後/提早退休」的天數
- **複利計算**：考慮通膨率和投資報酬率，計算實質報酬率
- **週期性支出**：一次性 vs 每月固定支出，展示訂閱服務的真正代價

### 🆕 GPS 導航系統
- **目標 vs 預估**：目標退休年齡固定，預估年齡根據行為動態計算
- **即時反饋**：每筆記錄都會更新「領先/落後」狀態
- **視覺化進度條**：直觀顯示與目標的差距

### 🆕 退休計算機（設定頁）
- **年齡導向**：設定退休年齡，計算可累積多少退休金
- **金額導向**：設定目標金額，計算需要工作到幾歲
- **生活品質導向**：設定退休後月領金額，計算所需存款（4%法則）

### 記錄追蹤
- 💸 **花費記錄**：追蹤消費並看到對退休的影響
- 💰 **儲蓄記錄**：記錄每筆儲蓄，看著退休日期提前
- 📊 **歷史統計**：按月分組查看，累積進度一目瞭然

### 資料儲存
- 📱 **本地儲存**：使用 localStorage，關閉瀏覽器也不會遺失
- ☁️ **雲端備份**：連接 Google Sheets，資料永久保存

---

## 🚀 快速開始

### 方法一：GitHub Pages 部署

1. **Fork 這個專案**
2. **啟用 GitHub Pages**
   - `Settings` → `Pages` → Source 選 `main` → Save
3. **完成！** 訪問 `https://你的帳號.github.io/timebar/`

### 方法二：本地使用

直接用瀏覽器開啟 `index.html` 即可。

---

## ☁️ 設定 Google Sheets 雲端備份

### 步驟 1：建立 Google Sheets

1. 開啟 [Google Sheets](https://sheets.google.com)
2. 建立新的空白試算表
3. **複製試算表 ID**（從網址中取得）：
   ```
   https://docs.google.com/spreadsheets/d/【這一段就是ID】/edit
   ```
   例如：`1bJ-plKk3locg4fRWtEcdL5rIgT2KQvEz5Xer1h7--MU`

### 步驟 2：建立 Apps Script

1. 在試算表中，點選 **擴充功能** → **Apps Script**
2. 刪除預設的程式碼
3. 複製 `google-apps-script/Code.gs` 的內容並貼上
4. **重要！** 在第 15 行填入你的試算表 ID：
   ```javascript
   const SPREADSHEET_ID = '貼上你的試算表ID';
   ```
5. 點擊 **儲存** (Ctrl+S)

### 步驟 3：測試連接

1. 在函數下拉選單選擇 `testConnection`
2. 點擊 ▶️ 執行
3. 首次會要求授權，點「授權存取」→「繼續」→ 選擇帳號 →「允許」
4. 查看執行紀錄，應顯示 `✅ 連接成功！`

### 步驟 4：部署為網頁應用程式

1. 點選 **部署** → **新增部署作業**
2. 點擊齒輪圖示，選擇 **網頁應用程式**
3. 設定：
   - 執行身分：**我**（不要改）
   - 誰可以存取：**所有人**
4. 點擊 **部署**
5. **複製產生的網址**

### 步驟 5：連接到 TimeBar

1. 開啟 `index.html`
2. 找到約第 165 行：
   ```javascript
   const GAS_WEB_APP_URL = '';
   ```
3. 貼上你的網址：
   ```javascript
   const GAS_WEB_APP_URL = 'https://script.google.com/macros/s/xxx.../exec';
   ```
4. 儲存並部署到 GitHub Pages（或直接本地開啟）

### 驗證成功

- App 設定頁面會顯示「Google Sheets 已連接」
- 記錄消費/儲蓄後，Google Sheets 會自動新增資料
- 「消費紀錄」分頁：綠色=儲蓄、紅色=花費
- 「使用者資料」分頁：包含每月儲蓄、目標退休金、通膨率、投資報酬率

> ⚠️ **升級注意**：從 v2.1 升級需刪除 Google Sheets 中的「使用者資料」分頁，讓系統重新建立新格式。

---

## 📁 專案結構

```
timebar/
├── index.html                  # 主應用程式
├── google-apps-script/
│   └── Code.gs                 # Google Apps Script 後端
└── README.md
```

---

## 🧮 計算邏輯

### 公式

**一次性消費/儲蓄：**
```
未來價值 = 金額 × (1 + 3.41%)^距離退休年數
時間成本 = 未來價值 ÷ 時薪
```

**每月固定消費/儲蓄：**
```
未來價值 = 月付金額 × ((1 + 月報酬率)^月數 - 1) ÷ 月報酬率
時間成本 = 未來價值 ÷ 時薪
```

### 預設參數

| 參數 | 預設值 | 可調整 | 說明 |
|------|--------|--------|------|
| 通膨率 | 2.5% | ✅ 0-10% | 長期平均 |
| 投資報酬率 | 6% | ✅ 0-15% | 大盤長期平均 |
| 實質報酬率 | ≈3.41% | 自動計算 | (1+ROI)/(1+通膨)-1 |
| 每月工作天數 | 22 天 | ❌ | - |
| 每日工時 | 8 小時 | ❌ | - |

---

## ❓ 常見問題

### Q: Google Sheets 沒有更新？
1. 確認 `SPREADSHEET_ID` 是否正確填入
2. 確認部署後有選擇「新版本」
3. 執行 `testConnection` 測試連接

### Q: 每次記錄會觸發兩個請求？
正常！一個儲存使用者資料，一個儲存消費紀錄。

### Q: 如何更新 Apps Script？
修改程式碼後：**部署** → **管理部署作業** → ✏️ **編輯** → 版本選「新版本」→ **部署**

### Q: 清除資料後頁面沒反應？
v2.1 已修正此問題，會自動跳轉到初始設定頁面。

### Q: 可以調整通膨率和報酬率嗎？
可以！在設定頁面的「計算參數」區塊可以自訂。

---

## 📄 License

MIT License

---

> 「每一分錢都是你未來自由的磚塊」🧱

Made with ❤️ for financial freedom
