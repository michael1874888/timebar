# TimeBar UI/UX 深度分析與重構計劃

> **文檔版本**: v1.0
> **日期**: 2026-01-06
> **目標**: 將 TimeBar 從「功能複雜的財務工具」轉型為「30 秒上手的時間成本可視化應用」

---

## 📊 執行摘要

### 核心發現

TimeBar 擁有獨特且強大的價值主張：**將金錢轉換為時間成本，幫助用戶做出更明智的消費決策**。然而，這個核心價值被埋藏在過於複雜的介面中。

**現狀問題：**
- 9 個主畫面 + 4 層導航深度
- Dashboard 有 8+ 個區塊競爭注意力
- 14 個財務概念需要理解
- 新用戶首次記帳需要 2 分鐘

**改進目標：**
- 3 個核心畫面
- 單一視覺焦點
- 3 個必須理解的概念
- 新用戶首次記帳 < 30 秒

### 用戶需求確認

基於與用戶的討論，確認以下設計方向：

| 維度 | 選擇 |
|------|------|
| **產品定位** | 平衡型應用（核心功能 + 適度遊戲化） |
| **視覺隱喻** | 探索新方案（不限於 Life Battery） |
| **使用頻率** | 中頻記帳（一天 1-3 次） |
| **設計原則** | 簡潔優先（寧可砍 50% 功能） |

---

## 第一部分：深度分析

## 1. 現狀全面盤點

### 1.1 功能清單

#### 核心功能（必須保留）

| 功能 | 位置 | 使用頻率 | 價值 |
|------|------|----------|------|
| 時間成本計算 | Dashboard, Tracker | 極高 | ⭐⭐⭐⭐⭐ 獨特價值主張 |
| 記帳（花費/儲蓄） | Dashboard, Tracker | 極高 | ⭐⭐⭐⭐⭐ 核心行為 |
| 退休 GPS 追蹤 | Dashboard, History | 高 | ⭐⭐⭐⭐⭐ 目標導向 |
| 歷史記錄查看 | History | 中 | ⭐⭐⭐⭐ 反思與學習 |
| 基本設定 | Settings | 低 | ⭐⭐⭐⭐ 個人化 |
| 雲端同步 | 背景 | 被動 | ⭐⭐⭐⭐ 跨裝置 |

#### 次要功能（可簡化）

| 功能 | 位置 | 使用頻率 | 問題 |
|------|------|----------|------|
| 遊戲化系統 | Dashboard, Shop | 低-中 | 感覺強制，應該是選配 |
| 每日挑戰 | Dashboard | 低 | 佔據太多空間 |
| 快速記帳按鈕 | Dashboard | 中 | 可以更精簡 |
| 分類管理 | Settings子頁 | 極低 | 過度設計 |
| 訂閱管理 | Settings子頁 | 低 | 導航太深 |

#### 冗餘功能（可移除）

| 功能 | 位置 | 問題 | 建議 |
|------|------|------|------|
| 每日預算小工具 | Dashboard | 與時間成本重複 | 移除 |
| Milestone 顯示 | Dashboard | 純粹數字，無感 | 移到 History |
| Catch-up Plan | Dashboard | 過於複雜 | 簡化為提示 |
| 免罪卡 | Shop, Tracker | 概念難懂 | 移除 |
| 3-mode 計算機 | Settings | 使用率極低 | 只保留1個 |

### 1.2 畫面結構分析

#### 當前架構（9 畫面）

```
TimeBar App
│
├─ 🎯 Onboarding (首次使用)
│   └─ 5 個步驟問答
│
├─ 🏠 Dashboard (主畫面)
│   ├─ Header (Logo + 積分 + 設定)
│   ├─ Life Battery (生命電池視覺化)
│   ├─ GPS Status (目標 vs 預估)
│   ├─ Milestone Display (累積時數)
│   ├─ Daily Budget Widget (每日額度)
│   ├─ Daily Challenge (4個挑戰)
│   ├─ Catch-up Plan (落後時顯示)
│   ├─ Quick Actions Bar (快速記帳)
│   ├─ Amount Input (金額輸入)
│   ├─ Time Cost Display (時間成本)
│   ├─ Decision Buttons (買了/不買)
│   └─ Bottom Navigation (4 按鈕)
│
├─ 📝 MainTracker (記帳頁)
│   ├─ GPS Header
│   ├─ Stats Summary
│   ├─ Mode Toggle (花費/儲蓄)
│   ├─ Amount Input + Slider
│   ├─ Category Grid (8-12 個分類)
│   ├─ Recurring Toggle
│   ├─ Note Field
│   ├─ Guilt-free Pass Toggle
│   └─ Submit Button
│
├─ 📊 History (歷史頁)
│   ├─ GPS Summary
│   ├─ Total Stats
│   ├─ Category Pie Chart
│   ├─ Monthly Groups
│   └─ Record List (可編輯/刪除)
│
├─ ⚙️ Settings (設定頁)
│   ├─ Personal Data (5 個參數)
│   ├─ Financial Calculator (3 模式)
│   ├─ Financial Parameters (通膨/ROI)
│   ├─ 4 個子頁面卡片:
│   │   ├─ Shop
│   │   ├─ Challenge Settings
│   │   ├─ Subscription Manager
│   │   └─ Category Settings
│   └─ Data Management
│
├─ 🛍️ Shop (商店頁)
│   ├─ Points Balance
│   ├─ Item Grid
│   └─ Purchase Confirmation
│
├─ 🎯 Challenge Settings (挑戰設定)
│   ├─ Default Challenges (可修改/刪除)
│   ├─ Custom Challenges (CRUD)
│   └─ Challenge Editor Modal
│
├─ 🏷️ Category Settings (分類設定)
│   ├─ Default Categories (可隱藏)
│   ├─ Custom Categories (CRUD)
│   └─ Category Editor Modal
│
├─ ⚡ Quick Actions Settings (快捷鍵設定)
│   ├─ Default Actions
│   ├─ Custom Actions (CRUD)
│   └─ Action Editor Modal
│
└─ 📱 Subscription Manager (訂閱管理)
    ├─ Active Subscriptions
    ├─ Ended Subscriptions
    └─ End Subscription Dialog
```

**問題診斷：**
- ❌ **導航深度過深**: 最深 4 層（Dashboard → Settings → Category Settings → Edit Modal）
- ❌ **功能重複**: Dashboard 和 Tracker 都能記帳，用戶困惑
- ❌ **注意力分散**: Dashboard 有 11 個視覺區塊
- ❌ **設定混亂**: 4 個獨立的設定子頁面，無清晰架構

### 1.3 用戶旅程分析

#### 情境 1：新用戶首次使用

```
目標：了解「買 $5000 的東西」會花掉多少人生時間

當前流程（7 步驟，~2 分鐘）：
1. 完成 Onboarding (5 個問題) ────────────── 60 秒
2. 看到 Dashboard ───────────────────────── 5 秒
3. 往下滾動找到金額輸入 ─────────────────── 10 秒
4. 輸入 5000 ────────────────────────────── 5 秒
5. 繼續往下滾動看時間成本 ────────────────── 10 秒
6. 理解顯示的資訊（多個指標） ─────────────── 20 秒
7. 決定買或不買 ─────────────────────────── 10 秒

問題點：
- ⏰ 時間過長（2 分鐘）
- 😵 需要滾動 2 次才看到完整資訊
- 🤔 資訊過載（Life Battery, GPS, Milestone 等干擾）
```

#### 情境 2：日常記帳

```
目標：記錄今天的午餐花費 $150

當前流程（2 種路徑）：

路徑 A：使用 Dashboard
1. 打開 App → Dashboard
2. 滾動到金額輸入
3. 輸入 150
4. 點擊「我買了」
5. (沒有分類選項，只能記為「一般消費」)

路徑 B：使用 Tracker
1. 打開 App → Dashboard
2. 點擊底部導航 → Tracker
3. 確認模式為「花費」
4. 輸入 150
5. 選擇分類「飲食」
6. 點擊「記錄」

問題點：
- 🤷 兩個入口，用戶不知道該用哪個
- 📝 Dashboard 快速但缺少分類
- 🐌 Tracker 完整但多一層導航
```

#### 情境 3：查看進度

```
目標：了解自己現在領先還是落後退休目標

當前流程：
1. Dashboard 可以看到 GPS 狀態
2. 但 Life Battery、GPS、Milestone 三個元件都顯示類似資訊
3. 用戶困惑：哪個才是「最重要」的指標？

問題點：
- 🎯 資訊重複
- 😕 優先級不清
```

### 1.4 認知負荷分析

#### 用戶必須理解的概念（按必要性排序）

| 概念 | 必要性 | 學習成本 | 當前說明 |
|------|--------|----------|----------|
| 時間成本 | ⭐⭐⭐⭐⭐ | 低 | 有（直觀） |
| 退休目標年齡 | ⭐⭐⭐⭐⭐ | 低 | 有 |
| GPS 追蹤 | ⭐⭐⭐⭐ | 中 | 不足 |
| 通膨率 | ⭐⭐⭐ | 高 | 無 |
| 投資報酬率 (ROI) | ⭐⭐⭐ | 高 | 無 |
| 實質報酬率 | ⭐⭐ | 極高 | 無 |
| 複利效應 | ⭐⭐⭐ | 中 | 隱含 |
| 年金計算 | ⭐⭐ | 高 | 隱含 |
| 4% 法則 | ⭐⭐ | 高 | 僅在計算機 |
| Life Battery 隱喻 | ⭐⭐⭐⭐ | 中 | 需要學習 |
| 訂閱 vs 一次性 | ⭐⭐⭐⭐ | 低 | 有 |
| 積分系統 | ⭐ | 低 | 有 |
| 免罪卡 | ⭐ | 高 | 不足 |
| 每日預算 | ⭐⭐ | 中 | 有 |

**總計：14 個概念，其中 5 個高學習成本**

**建議簡化至 3 個核心概念：**
1. 時間成本（錢 = 時間）
2. 退休目標追蹤（GPS）
3. 自由天數（用「天」為單位，不用小時或年）

---

## 2. 核心價值主張分析

### 2.1 TimeBar 的獨特優勢

與市面上其他記帳 App 的差異化比較：

| 維度 | 一般記帳 App | TimeBar |
|------|-------------|---------|
| **數據呈現** | 花了多少「錢」 | 花了多少「人生時間」 |
| **時間尺度** | 本月/本年 | 到退休為止（30+ 年） |
| **心理訴求** | 避免虧損 | 追求自由 |
| **計算方式** | 簡單加減 | 複利 + 未來價值 |
| **訂閱費用** | 顯示月付金額 | 顯示幾年生命代價 |
| **動機來源** | 罪惡感/控制 | 希望/賦能 |

**TimeBar 的超能力：讓抽象的財務決策變得「有感」**

### 2.2 心理學設計原則

TimeBar 採用的行為設計原則：

#### 2.2.1 時間比金錢更有感

**研究支持**:
- 人們對時間的感知比金錢更直觀
- "$500" vs "3 天的人生" → 後者更有衝擊力

**實作方式**:
```typescript
// lifeCostCalc.ts - 生動比喻
"這相當於一週的薪水"
"一個週末陪伴家人的時間"
"延後退休 5 天"
```

#### 2.2.2 正向心理學（不用羞辱）

**設計理念**:
- ❌ 不說「你超支了」
- ✅ 說「已記錄，覺察就是進步的開始」

**實作方式**:
```typescript
// messageFramework.ts
behind: {
  positive: '你正在追趕中，每一步都算數！',
  action: '試試今日挑戰，馬上開始進步'
}
```

#### 2.2.3 慶祝克制（不只是記錄）

**差異化**:
- 其他 App：記錄花費 → 純資訊
- TimeBar：選擇不買 → 🎉 慶祝 + 🎊 彩帶 + 💰 記為儲蓄

**心理效應**:
克制消費變成「獲得」（儲蓄），而非「失去」（放棄購買）

#### 2.2.4 未來自我連結

**心理技巧**:
```
"退休後的你會感謝現在的決定 🙏"
"你正在買回自己的時間 ⏰"
```

將當下決策與未來自我建立情感連結

### 2.3 視覺隱喻有效性分析

#### 當前：Life Battery（生命電池）

**概念**:
```
[==已活==|==工作中==|未來自由]
  灰色     橘色        綠色
```

**優點**:
- ✅ 視覺化生命分配
- ✅ 顏色分明
- ✅ 進度感強

**缺點**:
- ❌ 電池隱喻需要學習（為什麼是電池？）
- ❌ 三段式顏色意義需要解釋
- ❌ 數字太多（年齡、預估年齡、差距）
- ❌ 非通用隱喻（不同文化理解不同）

#### 評估替代方案

**方案 A：進度條 + 時間軸**
```
目標 65 歲                預估 63.2 歲
    ↓                         ↓
────●─────────────────●────────
   計劃路徑            實際進度

🚀 領先 1.8 年
```

**優點**:
- ✅ 通用隱喻（所有人都懂進度條）
- ✅ 領先/落後一目了然
- ✅ 只有 2 個關鍵數字
- ✅ 可擴展（加入里程碑標記）

**方案 B：地圖 GPS**
```
    你在這裡 📍
        ↓
起點 ═══●═══════════ 終點
    30歲    目標65歲   預估63.2歲

剩餘距離：33 年
領先：1.8 年 🚀
```

**優點**:
- ✅ 強化 GPS 隱喻（與產品名稱呼應）
- ✅ 旅程感強
- ✅ 容易理解

**方案 C：時鐘/倒數計時**
```
⏰ 距離財務自由還有
   33 年 2 個月

(但你可以提早 1.8 年！)
```

**優點**:
- ✅ 極度簡化
- ✅ 數字單一聚焦
- ✅ 倒數感製造緊迫性

**推薦：方案 A（進度條）**
- 最直觀，無需學習
- 與其他 UI 元素一致性高
- 易於響應式設計

---

## 3. 競品與最佳實踐

### 3.1 財務類 App 參考

| App | 核心概念 | 視覺隱喻 | 學習 |
|-----|---------|---------|------|
| YNAB | 給每一塊錢一個工作 | 信封系統 | ⭐⭐⭐ |
| Mint | 自動分類追蹤 | 儀表板 | ⭐⭐ |
| Clarity Money | 訂閱管理 | 列表 | ⭐ |
| **TimeBar** | 時間成本 | Life Battery → 進度條 | ⭐⭐ |

### 3.2 簡化設計典範

#### Duolingo（語言學習）
**做法**:
- 新用戶只看到「開始課程」按鈕
- 進階功能（排行榜、商店）漸進解鎖

**可借鏡**:
- 漸進式揭露策略
- 遊戲化為選配，不是必須

#### Headspace（冥想）
**做法**:
- 主畫面只有「開始」按鈕 + 今日主題
- 統計、課程列表都在次要位置

**可借鏡**:
- 單一行動呼籲（CTA）
- 減少選擇疲勞

#### Robinhood（投資）
**做法**:
- 複雜的金融工具用簡單的卡片呈現
- 進階圖表可選擇性查看

**可借鏡**:
- 漸進式資訊揭露
- 預設顯示最簡單的指標

---

## 第二部分：重構計劃

## 4. 新架構設計

### 4.1 設計原則

**P1. 極簡主義**
> 每個畫面只做一件主要的事

**P2. 即時反饋**
> 輸入 → 立即看到影響（< 0.5 秒）

**P3. 漸進揭露**
> 新用戶看到最簡版，資深用戶自動解鎖進階功能

**P4. 一致性**
> 退休進度條在所有畫面保持一致呈現

**P5. 行動導向**
> 文字用「你」而非「使用者」，問句而非陳述

### 4.2 新架構概覽

```
TimeBar App (新版)
│
├─ 🎯 Onboarding (簡化版)
│   └─ 3 個必要問題
│       ├─ 年齡
│       ├─ 月薪
│       └─ 目標退休年齡
│
├─ 🏠 主畫面：記帳台
│   │
│   ├─ [固定顯示]
│   │   ├─ Header (Logo + 積分badge + 設定icon)
│   │   ├─ 退休進度條 (替換 Life Battery)
│   │   ├─ 核心提問：「這筆花費會影響你的自由多久？」
│   │   ├─ 金額輸入（大 + 居中）
│   │   ├─ 時間成本顯示（超大字體）
│   │   └─ 決策按鈕（我買了 / 我忍住了）
│   │
│   └─ [漸進顯示]
│       ├─ 快速記帳按鈕 (3 筆記錄後)
│       └─ 今日挑戰 (第 2 天後，可收合)
│
├─ 📊 歷史：時間帳本
│   ├─ 退休進度條 (與主畫面一致)
│   ├─ 累計影響（用「天」為單位）
│   ├─ 消費分布圖表
│   ├─ 月度時間軸
│   └─ 記錄列表（可編輯）
│
└─ ⚙️ 設定：控制中心
    │
    ├─ [基本設定 - 始終顯示]
    │   ├─ 你的人生參數（年齡、薪水、目標）
    │   └─ 退休計算機（單一模式）
    │
    ├─ [進階設定 - Collapsible]
    │   ├─ > 快速記帳按鈕 (Modal)
    │   ├─ > 消費分類 (Modal)
    │   └─ > 訂閱管理 (Modal)
    │
    ├─ [遊戲化 - Collapsible + 可關閉]
    │   ├─ > 每日挑戰 (Modal)
    │   └─ > 時間沙商店 (Modal)
    │
    └─ [資料管理]
        ├─ 雲端同步狀態
        ├─ 匯出資料
        └─ 清除所有資料
```

**改進統計：**
- 9 個畫面 → **3 個核心畫面**（-67%）
- 4 層深度 → **2 層深度**（-50%）
- 11 個區塊 → **4 個核心區塊**（-64%）
- 14 個概念 → **3 個核心概念**（-79%）

### 4.3 主畫面詳細設計

#### Layout 結構

```
┌─────────────────────────────────────┐
│ [TimeBar Logo]    [🎯 150]  [⚙️]    │  ← Header (固定)
├─────────────────────────────────────┤
│                                     │
│  🎯 退休目標進度                    │  ← 進度條（30vh）
│  ━━━━━━━━━━━━━━●━━━━                │
│  目標 65 歲        預估 63.2 歲      │
│  🚀 領先 1.8 年                     │
│                                     │
│  (點擊展開詳細資訊)                  │
│                                     │
├─────────────────────────────────────┤
│                                     │
│  這筆花費會影響你的自由多久？        │  ← 核心問句
│                                     │
├─────────────────────────────────────┤
│                                     │
│    [____________] 元                │  ← 金額輸入
│                                     │     (60px 高度)
│    [100] [500] [1000] [5000]       │  ← 快速金額
│                                     │     (可左右滑動)
│    [ ] 每月持續（訂閱）              │  ← Toggle
│                                     │
├─────────────────────────────────────┤
│                                     │
│  ⏰ 這會花掉你：                     │  ← 時間成本
│                                     │     (當 amount > 0 顯示)
│     3.2 天                          │
│     的人生自由時間                   │  ← 超大字體 (48px)
│                                     │
│  = 相當於一週的薪水                  │  ← 生動比喻 (18px)
│  = 延後退休 3 天                     │  ← 退休影響 (18px)
│                                     │
├─────────────────────────────────────┤
│                                     │
│  [💸 我買了]    [💪 我忍住了]        │  ← 決策按鈕
│   (灰色)        (綠色 + 發光)        │     (固定底部)
│                                     │
└─────────────────────────────────────┘

   (往下滑動或展開)

   ⚡ 快速記帳                         ← 可展開區域
   [☕ 咖啡] [🍱 午餐] [🚇 交通] ...      (3+ 筆記錄後顯示)

   🎯 今日挑戰                         ← Collapsible
   □ 喝水取代飲料 (+10⏳)                (第 2 天後顯示)
   ✓ 自己煮飯 (+10⏳)                   (可完全收合)
   □ 步行通勤 (+10⏳)
```

#### 互動流程

**情境 A：查詢金額影響（不記錄）**
```
1. 用戶輸入金額：500
   ↓ (即時計算，< 0.1 秒)
2. 顯示時間成本：「0.8 天」
3. 用戶看完後可以：
   - 清空金額（不記錄）
   - 修改金額（重新計算）
   - 點擊決策按鈕（記錄）
```

**情境 B：記錄花費**
```
1. 用戶輸入：500
2. 點擊「💸 我買了」
   ↓
3. 彈出 Modal：
   ┌─────────────────────┐
   │ 選擇分類              │
   │ [🍽️ 飲食] [🚗 交通]  │
   │ [🎮 娛樂] [🏠 居住]  │
   │ ...                 │
   │                     │
   │ 備註(選填): ______   │
   │                     │
   │ [取消]  [確認記錄]   │
   └─────────────────────┘
4. 確認後：
   - Toast: 「已記錄消費 📝」
   - 觸發覺察動畫（粒子上升）
   - 進度條更新
```

**情境 C：慶祝克制**
```
1. 用戶輸入：5000
2. 看到「10.3 天的人生自由時間」
3. 點擊「💪 我忍住了」
   ↓
4. 全螢幕慶祝：
   ┌─────────────────────┐
   │   🎉                │
   │                     │
   │   太棒了！           │
   │                     │
   │   省下 $5,000       │  (綠色大字)
   │   = 10.3 天的自由    │
   │                     │
   │ [不記錄]  [記為儲蓄] │
   └─────────────────────┘
   (背景：彩帶動畫)
5. 若選「記為儲蓄」：
   - 記錄類別：「克制消費」
   - Toast: 「已記入儲蓄 💰」
   - 進度條往前推進
   - 獲得積分 +10⏳
```

#### 響應式設計

**手機版（< 768px）**
- 單欄布局
- 進度條高度 25vh
- 金額輸入欄寬度 100%
- 快速金額按鈕 4 個一排

**平板/桌面版（≥ 768px）**
- 最大寬度 600px 置中
- 進度條高度 30vh
- 快速金額按鈕 6 個一排

### 4.4 進度條元件設計

#### 視覺規格

```typescript
// src/components/dashboard/RetirementProgress.tsx

interface RetirementProgressProps {
  currentAge: number;
  targetAge: number;
  estimatedAge: number;
  yearsSaved: number;
}

// 視覺狀態
type ProgressState = 'ahead' | 'onTrack' | 'behind';

// 顏色定義
const stateColors = {
  ahead: {
    bar: '#10b981', // emerald-500
    dot: '#34d399', // emerald-400
    badge: 'bg-emerald-500',
    icon: '🚀'
  },
  onTrack: {
    bar: '#3b82f6', // blue-500
    dot: '#60a5fa', // blue-400
    badge: 'bg-blue-500',
    icon: '✅'
  },
  behind: {
    bar: '#f97316', // orange-500
    dot: '#fb923c', // orange-400
    badge: 'bg-orange-500',
    icon: '⏰'
  }
};
```

#### 佈局

```
┌──────────────────────────────────────┐
│  🎯 退休目標進度                      │  ← 標題
├──────────────────────────────────────┤
│                                      │
│  ●────────────────────────●──────    │  ← 進度條
│  │                        │          │
│  目標 65                預估 63.2    │  ← 標籤
│                                      │
│  [🚀 領先 1.8 年]                    │  ← 狀態 badge
│                                      │
│  (點擊查看詳情)                       │  ← 互動提示
└──────────────────────────────────────┘
```

#### 點擊展開詳情

```
┌──────────────────────────────────────┐
│  退休 GPS 分析                        │
├──────────────────────────────────────┤
│                                      │
│  目標退休年齡: 65 歲                  │
│  目前預估: 63.2 歲                    │
│                                      │
│  累積影響:                            │
│  • 總共省下: 120 天 ✅                │
│  • 總共花掉: 48 天 ⚠️                 │
│  • 淨值: +72 天 🎉                   │
│                                      │
│  這表示你可以提早 1.8 年退休！         │
│                                      │
│  [關閉]                              │
└──────────────────────────────────────┘
```

### 4.5 歷史頁面優化

#### 改進重點

**變更前（現狀）：**
- GPS 資訊與 Life Battery 重複
- 用「金額」顯示累計（$120,000）
- 月度匯總不明顯

**變更後：**
- 統一使用進度條（與主畫面一致）
- 用「天數」顯示累計（120 天自由時間）
- 月度匯總優先級提高

#### 新佈局

```
┌─────────────────────────────────────┐
│ [←] 時間帳本                        │
├─────────────────────────────────────┤
│                                     │
│  退休目標進度                        │  ← 與主畫面一致
│  ●────────────────────────●──────    │
│  目標 65 歲        預估 63.2 歲      │
│  🚀 領先 1.8 年                     │
│                                     │
├─────────────────────────────────────┤
│                                     │
│  總計影響                            │
│  ┌───────────────────────────────┐  │
│  │ 省下 120 天 ✅                 │  │
│  │ 花掉  48 天 ⚠️                 │  │
│  │ ────────────────────          │  │
│  │ 淨值  +72 天 🎉               │  │
│  └───────────────────────────────┘  │
│                                     │
├─────────────────────────────────────┤
│                                     │
│  ━━━ 消費分布 ━━━                   │
│  [圓餅圖：依分類顯示]                 │
│  • 飲食 40%                         │
│  • 交通 25%                         │
│  • 娛樂 20%                         │
│  • 其他 15%                         │
│                                     │
├─────────────────────────────────────┤
│                                     │
│  ━━━ 時間軸 ━━━                     │
│                                     │
│  2026年1月                          │
│  ├─ 淨影響: +15 天 🎉               │
│  ├─ 省下: 30 天                     │
│  └─ 花掉: 15 天                     │
│                                     │
│  • 2026-01-05 儲蓄 $10,000  +2天 ✅ │
│  • 2026-01-04 飲食 $150     -0.3天  │
│  • 2026-01-03 交通 $500     -1天    │
│  ...                                │
│                                     │
│  2025年12月                         │
│  ├─ 淨影響: +8 天                   │
│  └─ ...                            │
│                                     │
└─────────────────────────────────────┘
  [編輯] [刪除] (點擊記錄顯示)
```

#### 記錄卡片設計

```
單筆記錄卡片：

┌─────────────────────────────────────┐
│ 🍽️ 午餐                             │  ← icon + 分類
│                                     │
│ $150                      -0.3 天   │  ← 金額 + 時間成本
│ 2026-01-06 12:30                   │  ← 時間戳
│ 備註：和同事聚餐                     │  ← 備註（若有）
│                                     │
│ [✏️ 編輯] [🗑️ 刪除]                 │  ← 操作按鈕
└─────────────────────────────────────┘
```

### 4.6 設定頁面扁平化

#### 改進策略

**現狀問題：**
- 4 個子頁面（Shop、Challenge Settings、Category Settings、Subscription Manager）
- 需要點擊卡片 → 進入新頁面 → 再點擊按鈕 → 彈出 Modal
- 深度：3-4 層

**新設計：**
- 全部改為 Collapsible 區塊 + Modal
- 深度：1-2 層
- 常用設定永久可見，進階設定預設收合

#### 新佈局

```
┌─────────────────────────────────────┐
│ [←] 設定                            │
├─────────────────────────────────────┤
│                                     │
│ ━━━ 你的人生參數 ━━━                │  [永久展開]
│                                     │
│ 年齡                                │
│ [30] ───●───────────────── [80]    │  ← Slider
│                                     │
│ 目標退休年齡                         │
│ [50] ───────●───────────── [80]    │
│                                     │
│ 月薪                                │
│ [$50,000] 📝                        │  ← 可編輯
│                                     │
│ 目前存款                             │
│ [$500,000] 📝                       │
│                                     │
│ 每月儲蓄                             │
│ [$10,000] 📝                        │
│                                     │
├─────────────────────────────────────┤
│                                     │
│ ━━━ 退休計算機 ━━━                  │  [永久展開]
│                                     │
│ [依年齡計算] [依金額] [依生活品質]   │  ← Tab 切換
│                                     │
│ 目前模式：依年齡計算                 │
│                                     │
│ 如果你 65 歲退休，基於:              │
│ • 目前存款 $500,000                 │
│ • 每月儲蓄 $10,000                  │
│ • 投資報酬 6%                       │
│                                     │
│ 你將累積：                           │
│ $2,340,000                         │  ← 大數字
│                                     │
│ 每月可領：                           │
│ $78,000 (4% 法則)                  │
│                                     │
├─────────────────────────────────────┤
│                                     │
│ ▼ 進階設定                          │  [Collapsible]
│                                     │  [預設收合]
│   > 快速記帳按鈕 ⚡                  │  ← 點擊開 Modal
│   > 消費分類管理 🏷️                 │
│   > 訂閱管理 📱                      │
│                                     │
├─────────────────────────────────────┤
│                                     │
│ ▼ 遊戲化設定                        │  [Collapsible]
│                                     │  [預設收合]
│                                     │  [可完全關閉]
│   積分餘額: 150 ⏳ | 道具: 2 🎫      │
│                                     │
│   > 每日挑戰設定 🎯                  │
│   > 時間沙商店 🛍️                   │
│                                     │
│   [關閉遊戲化功能]                   │  ← Toggle
│                                     │
├─────────────────────────────────────┤
│                                     │
│ ━━━ 資料與同步 ━━━                  │  [永久展開]
│                                     │
│ Google Sheets: ✅ 已連接            │
│ 最後同步: 2026-01-06 14:30          │
│                                     │
│ [立即同步]  [匯出 CSV]              │
│                                     │
├─────────────────────────────────────┤
│                                     │
│ ━━━ 危險區域 ━━━                    │  [紅色警示]
│                                     │
│ [🗑️ 清除所有資料]                   │  ← 需二次確認
│                                     │
└─────────────────────────────────────┘
```

#### Modal 設計（以快速記帳為例）

```
點擊「> 快速記帳按鈕」後彈出：

┌─────────────────────────────────────┐
│ 快速記帳按鈕設定               [✕]   │
├─────────────────────────────────────┤
│                                     │
│ 目前快捷鍵：                         │
│                                     │
│ ┌─────────────────────────────────┐ │
│ │ ☕ 咖啡 - $100      [編輯][刪除] │ │
│ └─────────────────────────────────┘ │
│                                     │
│ ┌─────────────────────────────────┐ │
│ │ 🍱 午餐 - $150      [編輯][刪除] │ │
│ └─────────────────────────────────┘ │
│                                     │
│ ┌─────────────────────────────────┐ │
│ │ 🚇 交通 - $35       [編輯][刪除] │ │
│ └─────────────────────────────────┘ │
│                                     │
│ ┌─────────────────────────────────┐ │
│ │ 🍩 點心 - $50       [編輯][刪除] │ │
│ └─────────────────────────────────┘ │
│                                     │
│ [+ 新增快捷鍵]                       │
│                                     │
│ ─────────────────────────────────   │
│                                     │
│ 💡 提示：快捷鍵會出現在主畫面底部，   │
│ 點擊一下即可快速記帳。               │
│                                     │
│               [完成]                 │
│                                     │
└─────────────────────────────────────┘
```

---

## 5. 功能取捨決策

### 5.1 保留（核心功能）

| 功能 | 理由 | 優先級 |
|------|------|--------|
| 時間成本計算 | 獨特價值主張 | P0 |
| 記帳（花費/儲蓄） | 核心行為 | P0 |
| 退休 GPS 追蹤 | 目標導向 | P0 |
| 歷史記錄 | 反思必須 | P1 |
| 基本設定 | 個人化必須 | P1 |
| 雲端同步 | 跨裝置需求 | P1 |

### 5.2 簡化（降級為選配）

| 功能 | 現狀 | 新設計 | 理由 |
|------|------|--------|------|
| 遊戲化系統 | 強制顯示 | 可關閉模組 | 不是所有人喜歡遊戲化 |
| 每日挑戰 | 佔據空間 | Collapsible | 新用戶不需要立刻看到 |
| 快速記帳 | 始終展開 | 3 筆後顯示 | 新用戶還沒養成習慣 |
| 分類管理 | 獨立頁面 | Modal | 使用頻率低 |
| 商店 | 獨立頁面 | Modal | 使用頻率低 |

### 5.3 移除（砍掉的功能）

| 功能 | 移除原因 | 補償方案 |
|------|----------|----------|
| 每日預算小工具 | 與時間成本重複，混淆核心概念 | 無（概念併入時間成本） |
| Milestone 顯示 | 純數字堆砌，無情感連結 | 移到歷史頁「總計影響」 |
| Catch-up Plan | 過於複雜，無法快速理解 | 簡化為「您落後 X 天，建議每月多存 $Y」 |
| 免罪卡 (Guilt-free Pass) | 概念難懂，使用率極低 | 移除（沒有好的替代，但不影響核心功能） |
| 3-mode 計算機 | 使用率低，3 個模式造成選擇疲勞 | 只保留最常用的「依年齡」模式 |

**移除功能的資料兼容性：**
- 現有用戶的免罪卡資料保留，但 UI 不顯示
- 可在「專家模式」中恢復（Phase 4 實作）

---

## 6. 漸進式揭露策略

### 6.1 設計理念

**問題**：TimeBar 有很多強大功能，但新用戶一次看到全部會overwhelm。

**解決方案**：根據用戶行為和使用天數，逐步解鎖功能。

### 6.2 解鎖邏輯

#### Day 1（新用戶）

**顯示：**
```
主畫面：
✅ 退休進度條（預設值）
✅ 金額輸入
✅ 時間成本顯示
✅ 決策按鈕

歷史：
✅ 進度條
✅ 空狀態提示「開始記帳後，這裡會顯示你的時間帳本」

設定：
✅ 基本參數
✅ 計算機
```

**隱藏：**
```
❌ 快速記帳按鈕
❌ 每日挑戰
❌ 遊戲化（積分、商店）
❌ 進階設定（分類、訂閱）
```

#### Day 2-3

**條件**：記錄 ≥ 3 筆

**解鎖：**
```
✅ 快速記帳按鈕（根據最常記錄的項目自動生成）
✅ 每日挑戰（預設收合）
```

**提示：**
```
Toast: 「🎉 解鎖：快速記帳按鈕！」
「我們注意到你常記錄『咖啡』和『午餐』，已為你建立快捷鍵！」
```

#### Day 7

**條件**：活躍天數 ≥ 7 且記錄 ≥ 10 筆

**解鎖：**
```
✅ 遊戲化功能（積分系統、挑戰、商店）
✅ 進階設定（分類管理、訂閱管理）
```

**提示：**
```
Modal:
「你已經連續使用 7 天了！🎉

為了讓記帳更有趣，我們為你開啟了：
• 積分系統 - 完成挑戰賺取積分
• 時間沙商店 - 用積分兌換獎勵
• 進階設定 - 客製化你的 TimeBar

[開始探索] [暫時跳過]
```

#### Day 30（資深用戶）

**條件**：活躍天數 ≥ 30

**解鎖：**
```
✅ 專家模式（可在設定開啟）
✅ 所有進階功能
✅ 自訂主題（未來功能）
```

### 6.3 實作方式

```typescript
// src/utils/progressiveDisclosure.ts

import { UserData, Record } from '@/types';

/**
 * 計算用戶使用天數
 */
function calculateDaysSinceOnboarding(userData: UserData): number {
  // 假設 userData 有 createdAt 欄位
  const createdAt = new Date(userData.createdAt || Date.now());
  const now = new Date();
  const diffMs = now.getTime() - createdAt.getTime();
  return Math.floor(diffMs / (1000 * 60 * 60 * 24));
}

/**
 * 漸進式揭露規則
 */
export const ProgressiveFeatures = {
  /**
   * 快速記帳按鈕
   * 條件：記錄 ≥ 3 筆
   */
  quickActions: (userData: UserData, records: Record[]): boolean => {
    return records.length >= 3;
  },

  /**
   * 每日挑戰
   * 條件：使用 ≥ 2 天
   */
  challenges: (userData: UserData): boolean => {
    return calculateDaysSinceOnboarding(userData) >= 2;
  },

  /**
   * 遊戲化功能（積分、商店）
   * 條件：使用 ≥ 7 天 且 記錄 ≥ 10 筆
   */
  gamification: (userData: UserData, records: Record[]): boolean => {
    const daysActive = calculateDaysSinceOnboarding(userData);
    return daysActive >= 7 && records.length >= 10;
  },

  /**
   * 進階設定（分類、訂閱）
   * 條件：使用 ≥ 7 天
   */
  advancedSettings: (userData: UserData): boolean => {
    return calculateDaysSinceOnboarding(userData) >= 7;
  },

  /**
   * 專家模式
   * 條件：使用 ≥ 30 天
   */
  expertMode: (userData: UserData): boolean => {
    return calculateDaysSinceOnboarding(userData) >= 30;
  },
};

/**
 * 檢查是否應該顯示解鎖通知
 */
export function checkUnlockNotification(
  userData: UserData,
  records: Record[]
): {
  shouldShow: boolean;
  feature: string;
  message: string;
} | null {
  // 檢查 localStorage 是否已顯示過
  const shownNotifications = JSON.parse(
    localStorage.getItem('shown_unlock_notifications') || '[]'
  );

  // 檢查快速記帳解鎖
  if (
    ProgressiveFeatures.quickActions(userData, records) &&
    !shownNotifications.includes('quickActions')
  ) {
    return {
      shouldShow: true,
      feature: 'quickActions',
      message: '🎉 解鎖：快速記帳按鈕！',
    };
  }

  // 檢查遊戲化解鎖
  if (
    ProgressiveFeatures.gamification(userData, records) &&
    !shownNotifications.includes('gamification')
  ) {
    return {
      shouldShow: true,
      feature: 'gamification',
      message: '你已經連續使用 7 天了！開啟遊戲化功能？',
    };
  }

  return null;
}
```

#### 在元件中使用

```tsx
// src/components/dashboard/DashboardScreen.tsx

import { ProgressiveFeatures } from '@/utils/progressiveDisclosure';

export function DashboardScreen({ userData, records, ... }) {
  // 檢查功能解鎖狀態
  const showQuickActions = ProgressiveFeatures.quickActions(userData, records);
  const showChallenges = ProgressiveFeatures.challenges(userData);
  const showGamification = ProgressiveFeatures.gamification(userData, records);

  return (
    <div>
      {/* 核心功能：始終顯示 */}
      <RetirementProgress {...} />
      <AmountInput {...} />
      <TimeCostDisplay {...} />
      <DecisionButtons {...} />

      {/* 條件功能：漸進顯示 */}
      {showQuickActions && <QuickActionsBar {...} />}
      {showChallenges && (
        <Collapsible title="今日挑戰" defaultOpen={false}>
          <DailyChallenge {...} />
        </Collapsible>
      )}
    </div>
  );
}
```

---

## 7. 實作路線圖

### 7.1 Phase 1: 主畫面重構（1-2 週）

**優先級：最高**

#### 目標
合併 Dashboard + Tracker 為單一「記帳台」

#### 關鍵檔案

| 檔案 | 動作 | 工作量 |
|------|------|--------|
| `src/components/dashboard/DashboardScreen.tsx` | 完全重寫 | 3 天 |
| `src/components/dashboard/RetirementProgress.tsx` | 新建 | 1 天 |
| `src/components/tracker/MainTracker.tsx` | 併入 Dashboard | 2 天 |
| `src/components/App.tsx` | 移除 Tracker 路由 | 0.5 天 |
| `src/utils/progressiveDisclosure.ts` | 新建 | 1 天 |

#### 具體任務

**Task 1.1: 建立 RetirementProgress 元件**
```typescript
// src/components/dashboard/RetirementProgress.tsx
// 替換 LifeBattery.tsx

- [ ] 設計進度條 UI
- [ ] 實作狀態計算（ahead/onTrack/behind）
- [ ] 實作顏色系統
- [ ] 新增點擊展開詳情功能
- [ ] 編寫單元測試
```

**Task 1.2: 重寫 DashboardScreen**
```typescript
// src/components/dashboard/DashboardScreen.tsx

移除：
- [ ] <LifeBattery /> → 用 <RetirementProgress /> 替換
- [ ] <MilestoneDisplay /> → 移到 History
- [ ] <DailyBudgetWidget /> → 完全刪除
- [ ] <CatchUpPlan /> → 簡化為文字提示
- [ ] 底部 4 按鈕導航 → 改為 2 按鈕

新增：
- [ ] <RetirementProgress /> 元件
- [ ] 重新排版金額輸入（居中、放大）
- [ ] 時間成本顯示（超大字體）
- [ ] 核心問句文字

整合 Tracker 功能：
- [ ] 點擊「我買了」彈出分類選擇 Modal
- [ ] 合併 Tracker 的記帳邏輯
```

**Task 1.3: 建立分類選擇 Modal**
```typescript
// src/components/dashboard/CategorySelectModal.tsx (新建)

- [ ] 分類網格佈局
- [ ] 備註欄位（選填）
- [ ] 確認/取消按鈕
- [ ] 與 CategorySystem 整合
```

**Task 1.4: 更新 App.tsx 路由**
```typescript
// src/components/App.tsx

- [ ] 移除 'tracker' Screen type
- [ ] 移除 MainTracker 元件引用
- [ ] 更新 screen 狀態管理
- [ ] 測試路由切換
```

**Task 1.5: 測試 & 優化**
```
- [ ] 手機瀏覽器測試（iOS Safari, Android Chrome）
- [ ] 平板測試（iPad）
- [ ] 桌面測試（Chrome, Firefox, Edge）
- [ ] 效能優化（< 100ms 輸入反應）
- [ ] 無障礙測試（鍵盤導航、螢幕閱讀器）
```

#### 驗收標準
- ✅ 新用戶從打開 App 到第一筆記帳 < 30 秒
- ✅ 金額輸入即時顯示時間成本（< 0.1 秒）
- ✅ 進度條清晰顯示領先/落後狀態
- ✅ 通過所有瀏覽器測試

---

### 7.2 Phase 2: 導航簡化（1 週）

**優先級：高**

#### 目標
9 個畫面 → 3 個核心畫面

#### 關鍵檔案

| 檔案 | 動作 | 工作量 |
|------|------|--------|
| `src/types/index.ts` | 更新 Screen type | 0.5 天 |
| `src/components/App.tsx` | 路由重構 | 2 天 |
| `src/components/settings/SettingsPage.tsx` | 子頁面改 Modal | 2 天 |
| `src/components/common/Modal.tsx` | 新建統一 Modal | 1 天 |

#### 具體任務

**Task 2.1: 更新 Screen type**
```typescript
// src/types/index.ts

舊的：
export type Screen = 'loading' | 'onboarding' | 'dashboard' |
  'tracker' | 'history' | 'settings' | 'shop' |
  'challenge-settings' | 'subscription-manager' |
  'category-settings' | 'quick-actions-settings';

新的：
export type Screen = 'loading' | 'onboarding' |
  'home' | 'history' | 'settings';

- [ ] 更新 Screen type 定義
- [ ] 檢查所有使用 Screen type 的地方
- [ ] 更新相關測試
```

**Task 2.2: App.tsx 路由重構**
```typescript
// src/components/App.tsx

- [ ] 移除 Shop、ChallengeSettings、CategorySettings 等路由
- [ ] 改用 Modal 狀態管理
- [ ] 更新底部導航（2 按鈕）
- [ ] 實作 Modal 開關邏輯
```

**Task 2.3: 建立統一 Modal 元件**
```typescript
// src/components/common/Modal.tsx

- [ ] 基礎 Modal 結構（遮罩、內容區、關閉按鈕）
- [ ] 滑入/滑出動畫
- [ ] ESC 鍵關閉
- [ ] 點擊遮罩關閉
- [ ] 響應式設計（手機全螢幕，桌面居中）
```

**Task 2.4: 設定子頁面改為 Modal**
```typescript
// src/components/settings/SettingsPage.tsx

- [ ] 移除獨立子頁面的路由邏輯
- [ ] 改用 Modal 開關狀態
- [ ] 更新「快速記帳」、「分類」、「訂閱」等卡片為按鈕
- [ ] 實作 Modal 內容（ChallengeSettings, CategorySettings, etc.）
```

#### 驗收標準
- ✅ Screen type 只有 5 個值
- ✅ 所有設定功能可從 Settings 頁面 Modal 訪問
- ✅ 無獨立的設定子頁面
- ✅ Modal 動畫流暢（60 fps）

---

### 7.3 Phase 3: 設定頁扁平化（1 週）

**優先級：中**

#### 目標
所有設定在單一頁面，使用 Collapsible 區塊

#### 關鍵檔案

| 檔案 | 動作 | 工作量 |
|------|------|--------|
| `src/components/settings/SettingsPage.tsx` | 重寫為扁平結構 | 3 天 |
| `src/components/common/Collapsible.tsx` | 新建 | 1 天 |
| 各 Settings 子元件 | 改為 Modal 內容 | 2 天 |

#### 具體任務

**Task 3.1: 建立 Collapsible 元件**
```typescript
// src/components/common/Collapsible.tsx

- [ ] 可展開/收合區塊
- [ ] 箭頭圖示動畫
- [ ] defaultOpen prop
- [ ] 儲存展開狀態到 localStorage
```

**Task 3.2: 重寫 SettingsPage**
```typescript
// src/components/settings/SettingsPage.tsx

佈局：
- [ ] 人生參數區塊（永久展開）
- [ ] 退休計算機區塊（永久展開）
- [ ] 進階設定區塊（Collapsible，預設收合）
- [ ] 遊戲化區塊（Collapsible，預設收合）
- [ ] 資料與同步區塊（永久展開）
- [ ] 危險區域（永久展開）

功能：
- [ ] 3-mode 計算機簡化為 1 個模式
- [ ] 「關閉遊戲化」toggle
- [ ] 各子功能用 Modal 開啟
```

**Task 3.3: 優化計算機**
```typescript
// src/components/settings/RetirementCalculator.tsx

移除：
- [ ] 「依金額」模式
- [ ] 「依生活品質」模式

保留：
- [ ] 「依年齡」模式（最常用）

新增：
- [ ] 即時計算結果（不需要點擊「計算」按鈕）
```

#### 驗收標準
- ✅ 所有設定在單一滾動頁面
- ✅ 進階功能預設收合
- ✅ 遊戲化可完全關閉
- ✅ 計算機只有 1 個模式，結果即時顯示

---

### 7.4 Phase 4: 漸進式揭露（1 週）

**優先級：中**

#### 目標
新用戶看到極簡版，資深用戶自動解鎖進階功能

#### 關鍵檔案

| 檔案 | 動作 | 工作量 |
|------|------|--------|
| `src/utils/progressiveDisclosure.ts` | 新建（已在 Phase 1） | - |
| `src/components/dashboard/DashboardScreen.tsx` | 應用邏輯 | 2 天 |
| `src/components/common/UnlockNotification.tsx` | 新建 | 1 天 |
| `src/types/index.ts` | 新增 UserData.createdAt | 0.5 天 |

#### 具體任務

**Task 4.1: 建立解鎖通知元件**
```typescript
// src/components/common/UnlockNotification.tsx

- [ ] Toast 樣式通知
- [ ] 慶祝動畫（粒子效果）
- [ ] 功能說明文字
- [ ] 「開始探索」/ 「暫時跳過」按鈕
```

**Task 4.2: DashboardScreen 應用邏輯**
```typescript
// src/components/dashboard/DashboardScreen.tsx

- [ ] 使用 ProgressiveFeatures 檢查解鎖狀態
- [ ] 條件渲染 QuickActionsBar
- [ ] 條件渲染 DailyChallenge
- [ ] 檢查並顯示解鎖通知
```

**Task 4.3: UserData 新增 createdAt**
```typescript
// src/types/index.ts

export interface UserData {
  // ...existing fields
  createdAt?: string; // ISO timestamp
}

// 遷移邏輯
- [ ] 現有用戶設定 createdAt = 當前時間（視為老用戶）
- [ ] 新用戶在 Onboarding 時自動設定
- [ ] 更新 Storage.save/load 邏輯
```

**Task 4.4: 測試解鎖流程**
```
- [ ] 新用戶（Day 1）只看到核心功能
- [ ] 記錄 3 筆後自動顯示快速記帳
- [ ] Day 2 自動顯示挑戰
- [ ] Day 7 彈出遊戲化解鎖通知
```

#### 驗收標準
- ✅ 新用戶初次使用只看到 4 個區塊
- ✅ 解鎖通知在正確時機顯示
- ✅ 解鎖狀態持久化（重新整理不會重置）
- ✅ 老用戶（v3.1）升級後保持所有功能可見

---

### 7.5 Phase 5: Onboarding 簡化（0.5 週）

**優先級：低**

#### 目標
5 個問題 → 3 個問題

#### 關鍵檔案

| 檔案 | 動作 | 工作量 |
|------|------|--------|
| `src/components/onboarding/OnboardingScreen.tsx` | 簡化流程 | 1 天 |
| `src/types/index.ts` | 更新 UserData 預設值 | 0.5 天 |

#### 具體任務

**Task 5.1: 簡化 Onboarding 流程**
```typescript
// src/components/onboarding/OnboardingScreen.tsx

移除問題：
- [ ] 「目前存款多少？」→ 預設 $0
- [ ] 「每月儲蓄目標？」→ 自動計算為薪水 × 20%

保留問題：
- [ ] 年齡
- [ ] 月薪
- [ ] 目標退休年齡

自動設定：
- [ ] 通膨率 = 2.5%
- [ ] ROI = 6%
- [ ] 每月儲蓄 = 月薪 × 0.2
- [ ] 目前存款 = 0
```

**Task 5.2: 更新預設值邏輯**
```typescript
// 在 handleOnboardingComplete 中：

const userData: UserData = {
  age,
  salary,
  retireAge,
  currentSavings: 0, // 預設
  monthlySavings: Math.round(salary * 0.2), // 自動計算
  inflationRate: 2.5, // 預設
  roiRate: 6, // 預設
  createdAt: new Date().toISOString(),
};
```

**Task 5.3: 新增「稍後設定」提示**
```typescript
// Onboarding 完成後的提示

Toast:
「我們已為你設定預設值（通膨 2.5%、報酬率 6%），
你可以稍後在設定中調整。」
```

#### 驗收標準
- ✅ Onboarding 只有 3 個步驟
- ✅ 完成時間 < 30 秒
- ✅ 預設值合理（不會出現極端情況）
- ✅ 用戶可在設定中修改所有參數

---

### 7.6 Timeline 總覽

```
Week 1-2: Phase 1 (主畫面重構)
├─ Week 1: RetirementProgress + DashboardScreen 重寫
└─ Week 2: 測試 + 優化

Week 3: Phase 2 (導航簡化)
├─ 更新 Screen type
├─ App.tsx 路由重構
└─ 建立 Modal 系統

Week 4: Phase 3 (設定頁扁平化)
├─ Collapsible 元件
├─ SettingsPage 重寫
└─ 計算機簡化

Week 5: Phase 4 (漸進式揭露)
├─ 解鎖通知元件
├─ 應用漸進邏輯
└─ 測試解鎖流程

Week 6: Phase 5 (Onboarding 簡化)
└─ 簡化為 3 步驟

Week 7-8: 整合測試 & 上線
├─ A/B 測試準備
├─ 用戶測試
├─ Bug 修復
└─ 正式上線
```

---

## 8. 測試計畫

### 8.1 A/B 測試設計

#### 測試組設定

**Control (A組)：現有版本**
- 維持 9 個畫面
- Life Battery 視覺
- 所有功能可見

**Treatment (B組)：新版本**
- 3 個核心畫面
- 進度條視覺
- 漸進式揭露

#### 流量分配
- A組：50%
- B組：50%
- 時長：2 週

#### 主要指標

| 指標 | 目標 | 測量方式 |
|------|------|----------|
| 新用戶首次記帳時間 | < 30 秒 | 從 Onboarding 完成到第一筆記錄的時間戳差 |
| 7 天留存率 | > 40% | Day 7 活躍用戶 / Day 1 新用戶 |
| 每週記帳次數 | 增加 30% | 平均每用戶每週記錄數 |
| 功能使用率（核心） | > 80% | 使用過「時間成本計算」的用戶比例 |
| 功能使用率（進階） | > 20% | 使用過「遊戲化」功能的用戶比例 |

#### 次要指標

| 指標 | 目標 |
|------|------|
| 平均每筆記錄完成時間 | < 15 秒 |
| 退休年齡改善 | 平均提早 > 0.5 年 |
| 設定頁訪問次數 | 每用戶 > 2 次/週 |
| NPS（淨推薦值） | > 30 |
| 主觀易用性評分 | > 4.0 / 5.0 |

### 8.2 用戶測試腳本

#### 測試對象
- **新用戶**（5 人）：從未用過 TimeBar
- **輕度用戶**（3 人）：使用 < 1 個月
- **重度用戶**（2 人）：使用 > 3 個月

#### 情境 1：首次使用（新用戶）

**任務：**
1. 完成 Onboarding
2. 查詢「買 $5000 的東西」的時間成本
3. 記錄一筆消費

**觀察重點：**
- ⏱️ 完成時間（目標 < 2 分鐘）
- 🤔 是否有困惑的時刻？
- 👆 是否需要滾動才能完成？
- 💬 用戶對進度條的反應

**訪談問題：**
1. 你覺得「退休進度條」清楚嗎？
2. 時間成本的顯示（X 天）有感嗎？
3. 有哪些地方讓你困惑？
4. 你會想繼續使用這個 App 嗎？為什麼？

#### 情境 2：日常記帳（輕度用戶）

**任務：**
1. 記錄今天的午餐 $150
2. 記錄今天的交通費 $50
3. 查看今天的累積影響
4. 查看本週的進度

**觀察重點：**
- ⚡ 使用快速記帳按鈕的意願
- 📊 是否會主動查看歷史頁面
- 🎯 是否注意到每日挑戰

**訪談問題：**
1. 快速記帳按鈕有幫助嗎？
2. 你想看到哪些資訊在主畫面？
3. 遊戲化功能（挑戰、積分）吸引你嗎？

#### 情境 3：進階功能（重度用戶）

**任務：**
1. 自訂一個快速記帳按鈕
2. 調整退休目標年齡
3. 管理訂閱項目
4. 查看 GPS 進度詳情

**觀察重點：**
- 🔍 能否快速找到進階設定？
- 😕 是否對功能被「隱藏」感到困擾？
- 💡 是否理解 Collapsible 區塊的設計

**訪談問題：**
1. 你覺得進階功能的位置合理嗎？
2. 漸進式揭露的設計是否影響你的使用？
3. 有哪些功能你覺得被移除/簡化後不方便？
4. 新版 vs 舊版，你更喜歡哪個？為什麼？

### 8.3 成功標準

**如果達成以下標準，視為重構成功：**

✅ **主要指標**
- 新用戶首次記帳時間 < 30 秒（從 ~2 分鐘降低 75%）
- 7 天留存率 > 40%（從 ~25% 提升 60%）
- 核心功能使用率 > 80%
- 進階功能使用率 > 20%

✅ **用戶滿意度**
- NPS > 30
- 易用性評分 > 4.0/5.0
- 至少 80% 的測試用戶表示「更喜歡新版」

✅ **技術指標**
- 輸入反應時間 < 100ms
- 頁面切換動畫 60 fps
- 無嚴重 bug（P0）
- 無中度 bug（P1）> 5 個

**如果未達成，降級策略：**
- 保留「經典模式」toggle
- 收集反饋，快速迭代
- 考慮分階段上線（先上 Phase 1-2）

---

## 9. 風險與緩解

### 9.1 風險識別

#### 風險 1：用戶不習慣新介面（高風險）

**影響**：留存率下降、負評增加

**緩解策略：**
1. **A/B 測試驗證**
   - 在全面推出前，用 50% 流量測試
   - 監控關鍵指標（留存、使用時長）

2. **提供「經典模式」**
   ```typescript
   // Settings 中新增 toggle
   [ ] 使用經典介面（9 畫面版本）
   ```

3. **Migration Guide**
   - 撰寫「What's New」頁面
   - 突出「更簡潔」、「更快速」的優勢
   - 提供舊功能位置的對照表

#### 風險 2：功能被砍太多，用戶抱怨（中風險）

**影響**：資深用戶流失

**緩解策略：**
1. **功能不是刪除，而是「隱藏」**
   - 所有功能都保留在程式碼中
   - 可透過「專家模式」恢復

2. **專家模式設計**
   ```
   Settings > 進階設定 >
   [ ] 啟用專家模式

   開啟後：
   - 所有功能永久可見
   - 不受漸進式揭露限制
   - 恢復 3-mode 計算機
   - 恢復免罪卡功能
   ```

3. **收集反饋並快速迭代**
   - 在 App 內嵌入「反饋」按鈕
   - 每週檢視用戶反饋
   - 2 週內釋出修正版本

#### 風險 3：開發時程過長（中風險）

**影響**：延遲上線、機會成本

**緩解策略：**
1. **分階段上線**
   - Phase 1-2 是 MVP（最小可行產品）
   - 先上線驗證核心假設
   - Phase 3-5 根據數據決定優先級

2. **並行開發**
   - Phase 1 和 Phase 2 可部分並行
   - 多人協作時分工明確

3. **快速原型驗證**
   - 用 Figma 製作可點擊原型
   - 先做 5 人用戶測試
   - 確認方向再開始編碼

#### 風險 4：漸進式揭露設計不當（低風險）

**影響**：新用戶誤以為功能不存在

**緩解策略：**
1. **明確的解鎖通知**
   - 每次解鎖都有慶祝動畫
   - 說明新功能的用途

2. **設定中顯示「未解鎖功能」**
   ```
   Settings > 進階設定 (鎖定)
   「使用 7 天後自動解鎖」
   ```

3. **提供手動解鎖選項**
   ```
   Settings > 進階
   [立即解鎖所有功能]
   ```

### 9.2 Rollback Plan（回滾計畫）

如果新版本出現嚴重問題，需要快速回滾：

**Step 1: 保留舊版本代碼**
```bash
git tag v3.1-stable
git branch legacy-ui
```

**Step 2: 功能開關**
```typescript
// src/utils/featureFlags.ts
export const USE_NEW_UI = process.env.VITE_USE_NEW_UI === 'true';

// 可透過環境變數快速切換
```

**Step 3: 快速回滾流程**
```bash
# 1. 切換環境變數
VITE_USE_NEW_UI=false

# 2. 重新建置
npm run build

# 3. 部署
# (根據你的部署流程)
```

**Step 4: 通知用戶**
```
Toast:
「我們暫時回到經典介面以修正問題，
新版本將在改進後重新推出。」
```

---

## 10. 後續規劃

### 10.1 短期（1-3 個月）

**完成核心重構後，可以考慮：**

1. **小型優化**
   - 新增更多生動比喻（「相當於 X 的代價」）
   - 優化動畫細節（彈性、緩動）
   - 深色模式優化

2. **功能補強**
   - 匯出 CSV 功能
   - 預算警示（接近目標時提醒）
   - 週報、月報功能

3. **測試與修正**
   - 根據 A/B 測試結果調整
   - 修正用戶反饋的問題
   - 效能優化

### 10.2 中期（3-6 個月）

**當核心體驗穩定後：**

1. **PWA 升級**
   - 離線支援
   - 通知推送（預算超支提醒）
   - 安裝提示

2. **Widget 開發**
   - iOS Homescreen Widget（顯示進度條）
   - Android Widget

3. **社交功能（可選）**
   - 匿名排行榜（省了多少天）
   - 分享退休進度到社群媒體

### 10.3 長期（6-12 個月）

**擴展價值主張：**

1. **Native App**
   - iOS / Android 原生 App
   - 系統整合（Siri Shortcuts, Widgets）

2. **瀏覽器擴充功能**
   - Chrome Extension
   - 在購物網站上自動顯示時間成本
   - 「Block Purchase」功能（需要用戶確認）

3. **AI 建議**
   - 分析消費模式
   - 提供個人化省錢建議
   - 「如果你少買 X，可以提早退休 Y 天」

4. **多語言支援**
   - 英文版本
   - 其他語言

---

## 附錄

### A. 設計參考資源

#### 顏色系統（Tailwind）

```javascript
// 狀態顏色
const colors = {
  // 領先（ahead）
  ahead: {
    primary: '#10b981', // emerald-500
    light: '#34d399',   // emerald-400
    dark: '#059669',    // emerald-600
  },

  // 準時（on track）
  onTrack: {
    primary: '#3b82f6', // blue-500
    light: '#60a5fa',   // blue-400
    dark: '#2563eb',    // blue-600
  },

  // 落後（behind）
  behind: {
    primary: '#f97316', // orange-500
    light: '#fb923c',   // orange-400
    dark: '#ea580c',    // orange-600
  },

  // 中性
  neutral: {
    bg: '#1f2937',      // gray-800
    text: '#f9fafb',    // gray-50
    border: '#374151',  // gray-700
  },
};
```

#### 字體尺寸

```javascript
// 時間成本顯示
fontSize: {
  'cost-primary': '48px',    // "3.2 天"
  'cost-secondary': '18px',  // "= 相當於..."
  'title': '24px',           // 區塊標題
  'body': '16px',            // 正文
  'caption': '14px',         // 說明文字
}
```

### B. 關鍵指標追蹤代碼

```typescript
// src/utils/analytics.ts

/**
 * 追蹤用戶行為的關鍵事件
 */
export const Analytics = {
  // 記錄首次記帳時間
  trackFirstRecord: (timeMs: number) => {
    // 從 Onboarding 完成到現在的時間差
    console.log('[Analytics] First record time:', timeMs);
    // 實際實作時整合 Google Analytics 或其他工具
  },

  // 記錄功能使用
  trackFeatureUsage: (feature: string) => {
    console.log('[Analytics] Feature used:', feature);
  },

  // 記錄解鎖事件
  trackFeatureUnlock: (feature: string, daysActive: number) => {
    console.log('[Analytics] Feature unlocked:', feature, 'days:', daysActive);
  },

  // 記錄 A/B 測試組別
  trackABTestGroup: (group: 'control' | 'treatment') => {
    console.log('[Analytics] AB Test Group:', group);
  },
};
```

### C. 相關文件連結

- **設計稿**: (待補充 Figma link)
- **用戶調查**: (待補充問卷連結)
- **技術文檔**: 見專案 README.md
- **API 文檔**: 見 google-apps-script/README.md

---

## 結語

TimeBar 擁有獨特且強大的價值主張，但這個價值被過於複雜的介面所掩蓋。這次重構的核心目標是：

> **讓「時間成本可視化」成為主角，其他一切都是配角。**

透過：
- 9 → 3 個畫面（簡化導航）
- Life Battery → 進度條（降低學習成本）
- 強制 → 選配遊戲化（尊重用戶偏好）
- 立即 → 漸進揭露（降低初始認知負荷）

我們期望達成：
- ✅ 新用戶 30 秒內完成首次記帳
- ✅ 7 天留存率提升至 40%
- ✅ 每週記帳次數增加 30%

**這不是刪減功能，而是重新組織資訊架構，讓核心價值更突出。**

準備好開始改變了嗎？讓我們讓 TimeBar 成為真正「30 秒上手」的時間成本可視化應用！🚀
